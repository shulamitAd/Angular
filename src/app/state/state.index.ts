import { ActionReducer, combineReducers, createSelector, createFeatureSelector } from '@ngrx/store';
//import { chartReducer, ChartState } from './reducers/chart.reducer';

// const reducers = {
//     chartState: chartReducer
// };

// interface CommonState {
//     chartState: ChartState;
// }
// const reducer: ActionReducer<CommonState> = combineReducers(reducers);

// function commonReducer(state: any, action: any) {
//     return reducer(state, action);
// }
// export { commonReducer, CommonState };

// export const getCommoneState = createFeatureSelector<CommonState>('common');

// export const getChartState = createSelector(getCommoneState,
//     (state: CommonState) => state.chartState);


